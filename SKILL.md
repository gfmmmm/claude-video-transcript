---
name: 대본추출
description: 영상 URL(인스타 릴스·유튜브·쇼츠·틱톡)을 받아 yt-dlp와 Whisper로 음성을 텍스트로 뽑고, 원본 대본과 자연스러운 한국어 번역을 함께 제공한다. "대본 뽑아줘", "영상 텍스트 추출", "스크립트 뽑아", "이 영상 뭐라고 해?", "번역해줘 이 영상", "/대본추출"에 반응.
---

# /대본추출 — 영상 URL → 원본 대본 + 한국어 번역

## 역할
영상 URL을 받아서 음성을 텍스트로 변환하고, 원본 대본과 한국어 번역을 함께 제공한다.

---

## 트리거
- 자연어: "대본 뽑아줘", "영상 텍스트 추출", "스크립트 뽑아", "이 영상 뭐라고 해?", "번역해줘 이 영상"
- 슬래시: `/대본추출`

---

## 지원 플랫폼
인스타그램 릴스, 유튜브, 유튜브 쇼츠, 틱톡 등 yt-dlp가 지원하는 모든 영상 URL

---

## 실행 절차

### 1단계 — URL 확인

사용자가 URL을 이미 제공한 경우 바로 2단계로 진입.
URL이 없으면 물어본다:

"영상 URL을 공유해주세요! 인스타 릴스, 유튜브, 틱톡 다 가능해요."

### 2단계 — 영상 다운로드

> ⚠️ **세션 충돌 방지** — 고정 파일명(`/tmp/transcript_work.*`)을 쓰면 여러 채팅에서 동시에 대본추출을 돌릴 때 서로의 오디오 파일을 덮어쓰거나 캐시된 파일을 재사용한다. URL별 고유 폴더 필수.

```bash
# 세션별 고유 작업 폴더 생성
URL="여기에_실제_URL"
WORK="/tmp/transcript_$(echo -n "$URL" | shasum -a 256 | cut -c1-12)"
rm -rf "$WORK" && mkdir -p "$WORK"

yt-dlp -f "bestaudio/best" --no-continue --force-overwrites -o "$WORK/audio.%(ext)s" "$URL" 2>&1
```

실패 시:
- 인스타그램에서 "login required" 또는 "rate-limit" 에러 → 브라우저 로그인 쿠키를 빌려 재시도: `--cookies-from-browser chrome` 옵션 추가 (Safari는 `safari`, Edge는 `edge`)
- 비공개 영상인지 확인 → "비공개 영상이라 접근이 안 돼요. 영상 파일을 직접 공유해주시겠어요?"
- yt-dlp 업데이트 필요 시 → `brew upgrade yt-dlp` (Mac) 또는 `pip install -U yt-dlp` 후 재시도

### 3단계 — 음성→텍스트 변환

Whisper로 텍스트 변환. `$WORK`는 2단계에서 만든 폴더 (새 bash 호출이면 동일 `URL`로 재계산):

```bash
whisper "$WORK"/audio.* --model base --language [자동감지 또는 영상 언어] --output_format txt --output_dir "$WORK"
```

언어 판단:
- 영상 설명/제목이 영어면 `--language en`
- 한국어면 `--language ko`
- 판단 어려우면 `--language` 생략 (자동 감지)

정확도가 필요한 경우 `--model small` 또는 `--model medium` 사용.
(처음 쓰는 모델은 첫 실행 때 한 번 다운로드되므로 몇 분 걸릴 수 있다. 사용자에게 미리 알려준다.)

### 4단계 — 결과 출력

아래 형식으로 출력한다:

```
## 원본 대본

> (원본 텍스트 — 문장 단위로 줄바꿈, 인용 블록)

## 한국어 번역

> (자연스러운 한국어 번역 — 직역보다 의역 우선, 원본 톤 유지)
```

원본이 한국어 영상이면 "한국어 번역" 섹션은 생략하고 원본 대본만 출력한다.

---

## 핵심 원칙

1. **"안 돼요" 금지** — URL이 안 되면 yt-dlp 업데이트, 쿠키 옵션, 다른 포맷 시도, 사용자에게 파일 요청 순으로 해결
2. **원본 보존** — 원본 대본은 있는 그대로 보여준다. 임의로 요약하거나 빼지 않는다
3. **자연스러운 번역** — 직역보다 한국어로 자연스럽게. 원본의 톤(유머, 진지함 등)을 살린다
4. **임시 파일 정리** — 작업 완료 후 `rm -rf "$WORK"`로 해당 세션 폴더만 삭제 (다른 채팅이 동시에 돌리고 있을 수 있으므로 `/tmp/transcript_*` 와일드카드 삭제 금지)

---

## 결과물 저장
- 기본적으로 화면에 출력만 한다 (파일 저장 안 함)
- 사용자가 "저장해줘"라고 하면 → 사용자가 지정한 위치에 `.md` 파일로 저장. 위치를 안 정해주면 현재 폴더에 `대본_YYYY-MM-DD_제목.md`로 저장
